# v0.3.0 Reliability Kernel I 验证记录

- 日期：2026-09-24
- 实现 PR：[PR #10](https://github.com/tommyxie2026-tech/computecloud/pull/10)
- 自动化证据：GitHub Actions run 36014049170（核心代码 commit 448743fad2fc82bf11160774af033568c32adb13）
- 范围：schema v4、Stage、multi-Attempt、generation/epoch fencing、Artifact gating

## 1. 自动化结果

| Gate | 结果 | 说明 |
| --- | --- | --- |
| go mod verify | PASS | 模块校验通过 |
| go vet ./... | PASS | 由 verify job 执行 |
| go test ./... -count=1 | PASS | 包含 v0.3 Stage/multi-Attempt/fencing 测试 |
| go test -race ./... -count=1 | PASS | 由 verify job 执行 |
| make smoke | PASS | Server/Worker 故障烟测回归 |
| make capacity-check | PASS | fixture 小容量矩阵回归 |
| make ci-flow | PASS | single、MapReduce、cancel、Worker failure recovery |

Run 36014049170 的 task-flow 与 verify 两个 job 均为 success。

## 2. v0.3 新增验证

新增测试覆盖：
- fresh schema v4；
- Task 多历史 Attempt；
- one-active Attempt schema invariant；
- dynamic generation 1 -> 2；
- stale generation event 被拒绝；
- old-generation Artifact 不发布；
- current-generation Artifact ACCEPTED；
- Stage READY -> RUNNING -> SUCCEEDED；
- Worker restart 后旧 epoch execution 被 fenced，同时允许 cleanup-only failure proof。

既有 v0.2 测试继续覆盖 Map/Reduce barrier、restart recovery、artifact/input authorization、cancel、MCP/G1 相关回归。

## 3. Release invariants

自动化当前支持以下结论：

~~~text
0 duplicate accepted executions
0 stale-generation accepted writes
0 invalid artifact publication
0 duplicated next Stage
~~~

这里的结论仅针对仓库自动化/fixture 覆盖范围，不扩大解释为真实供应商或跨主机生产证明。

## 4. 明确未完成的真实环境证据

以下仍由 Production Baseline Issue #1 跟踪，不能用 GitHub Actions fixture 代替：
- 真实 Codex；
- 真实 Claude；
- 真实 Codex MCP；
- 两台独立主机；
- 真实网络中断/恢复；
- 24h+ 真实 Job；
- 真实 Runtime 容量曲线；
- 真实上游计量对照。

因此当前状态是：**v0.3.0 implementation/automated validation complete; production release gate pending #1**。
